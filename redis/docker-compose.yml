services:
  redis:
    image: redis:latest
    container_name: redis
    hostname: redis
    networks:
      - backend
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:

networks:
  backend:
    external: true
